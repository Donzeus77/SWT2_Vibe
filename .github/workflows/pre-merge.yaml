name: Pre Merge Checks

on:
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    services:
      postgres-simulator:
        image: postgres:16
        env:
          POSTGRES_DB: simulator_db
          POSTGRES_USER: mensa
          POSTGRES_PASSWORD: mensa123
        ports:
          - 5433:5432
        options: >-
          --health-cmd "pg_isready -U mensa -d simulator_db"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      postgres-app:
        image: postgres:16
        env:
          POSTGRES_DB: mensa_db
          POSTGRES_USER: mensa
          POSTGRES_PASSWORD: mensa123
        ports:
          - 5434:5432
        options: >-
          --health-cmd "pg_isready -U mensa -d mensa_db"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Start Mosquitto
        run: |
          docker run -d --name mosquitto -p 1883:1883 \
            -v "$PWD/mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf" \
            eclipse-mosquitto:2
          for i in $(seq 1 20); do
            if docker exec mosquitto mosquitto_sub -t '$SYS/broker/version' -C 1 -W 2 > /dev/null 2>&1; then
              echo "Mosquitto is ready"
              break
            fi
            sleep 1
          done
      - name: Set up JDK
        uses: actions/setup-java@v4.5.0
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven
      - name: Build all Maven modules
        run: ./mvnw --batch-mode --update-snapshots verify

  discover-deployable-modules:
    runs-on: ubuntu-latest
    outputs:
      has-modules: ${{ steps.modules.outputs.has-modules }}
      modules: ${{ steps.modules.outputs.modules }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - id: modules
        name: Discover deployable Maven modules
        run: |
          python <<'PY'
          import json
          import os
          import xml.etree.ElementTree as ET


          def namespace(root):
              return {"m": root.tag.split("}", 1)[0][1:]} if root.tag.startswith("{") else {}


          def find(node, path):
              ns = namespace(node)
              lookup = path if ns else path.replace("m:", "")
              return node.find(lookup, ns)


          def findall(node, path):
              ns = namespace(node)
              lookup = path if ns else path.replace("m:", "")
              return node.findall(lookup, ns)


          def discover_modules(pom_path, module_prefix=""):
              pom_root = ET.parse(pom_path).getroot()
              modules = []

              for module in findall(pom_root, "m:modules/m:module"):
                  module_name = (module.text or "").strip()
                  if not module_name:
                      continue

                  module_path = os.path.normpath(os.path.join(module_prefix, module_name))
                  module_pom_path = os.path.join(module_path, "pom.xml")
                  module_root = ET.parse(module_pom_path).getroot()

                  enabled = find(module_root, "m:properties/m:container.image.enabled")
                  if enabled is not None and (enabled.text or "").strip().lower() == "true":
                      artifact_id = find(module_root, "m:artifactId")
                      image_name = (artifact_id.text or module_path).strip().replace("/", "-").lower()
                      modules.append({"path": module_path.replace(os.sep, "/"), "image": image_name})

                  modules.extend(discover_modules(module_pom_path, module_path))

              return modules


          modules = discover_modules("pom.xml")

          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output:
              output.write(f"has-modules={'true' if modules else 'false'}\n")
              output.write(f"modules={json.dumps(modules)}\n")
          PY

  container-check:
    needs: [verify, discover-deployable-modules]
    if: ${{ needs.discover-deployable-modules.outputs.has-modules == 'true' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        module: ${{ fromJson(needs.discover-deployable-modules.outputs.modules) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build deployable container
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.java
          build-args: |
            MODULE=${{ matrix.module.path }}
          push: false
